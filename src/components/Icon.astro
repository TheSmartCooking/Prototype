---
export interface Props {
    name: string;
}

const { name } = Astro.props as Props;
const { default: innerHTML } = await import(`/src/assets/icon/${name}.svg?raw`);
---

<Fragment set:html={innerHTML} />
